# Lore fixture commerce repository

This small PHP repository models the demo used by Lore. It includes deliberate OMS search behaviour, Avalara mapping boundaries, a refund regression, a Business Central consumer, focused tests, and historical commit/PR data in `fixtures/`.
